import { Injectable } from '@angular/core';

@Injectable()
export class AppSettingsService {
    public expertMode = false;
    constructor() { }
    restoreSettings() { } // @todo placeholder for now
    saveSettings() { } // @todo placeholder for now
}
